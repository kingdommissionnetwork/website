name = "website"
main = "backend/src/index.ts"
compatibility_date = "2026-08-31"
compatibility_flags = ["nodejs_compat"]

# Non-sensitive project URL — safe to commit.
# All secret keys (SUPABASE_SECRET_KEY, SUPABASE_PUBLISHABLE_KEY, JWT_SECRET, etc.)
# are stored as Cloudflare secrets via `wrangler secret put` and persist independently.
[vars]
SUPABASE_URL = "https://wvjoxhpoxytkjuuridpv.supabase.co"

[assets]
directory = "./dist"
not_found_handling = "single-page-application"
